#!/usr/bin/env bash

case "$GITHUB_EVENT_NAME" in
  push)
    echo "is_built=$(jq -r '.build' -)"
    ;;
  schedule)
    echo "is_built=$(jq -r '.buildOnSchedule' -)"
    ;;
  workflow_dispatch)
    echo "is_built=$(jq -r '.buildOnRequest' -)"
    ;;
  *)
    exit 1
    ;;
  esac
